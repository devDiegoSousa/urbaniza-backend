spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/urbanize
    username: urbanizausername
    password: urbanizapassword
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
